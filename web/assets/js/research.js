const form=document.getElementById('search-form');const results=document.getElementById('results');form.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);const q=fd.get('query');try{const hits=await api.post('/search/semantic',{query:q});results.innerHTML='';hits.forEach(h=>{const el=document.createElement('div');el.className='item';el.innerHTML=`<strong>${h.title}</strong><br/><small>score: ${h.score.toFixed(3)}</small><p>${h.snippet}...</p>`;results.appendChild(el)})}catch(err){results.textContent=err.message}});