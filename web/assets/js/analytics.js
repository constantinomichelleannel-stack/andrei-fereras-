const pForm=document.getElementById('predict-form');const sForm=document.getElementById('strategy-form');const pOut=document.getElementById('predict-result');const sOut=document.getElementById('strategy-result');pForm.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(pForm);const payload=Object.fromEntries(fd.entries());try{const r=await api.post('/predict/case-outcome',payload);pOut.textContent=JSON.stringify(r,null,2)}catch(err){pOut.textContent=err.message}});sForm.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(sForm);const payload=Object.fromEntries(fd.entries());try{const r=await api.post('/strategy/recommend',payload);sOut.textContent=JSON.stringify(r,null,2)}catch(err){sOut.textContent=err.message}});